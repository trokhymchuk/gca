name: Docker (static)

on:
  workflow_dispatch:

jobs:
  build:
    name: Build (static)
    environment: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Docker/static/Dockerfile
          target: final
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max

  test:
    name: Test (static)
    needs: build
    environment: test
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Docker/static/Dockerfile
          target: test
          load: true
          tags: gca-static-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker run --rm gca-static-test:latest

  publish:
    name: Publish (static)
    needs: test
    environment: publish
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Docker/static/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/gca-static:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
