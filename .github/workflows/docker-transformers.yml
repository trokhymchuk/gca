name: Transformers

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      step:
        description: 'Step to run. "build"/"test" run on their own; "publish" runs build → test → publish.'
        type: choice
        default: test
        options:
          - build
          - test
          - publish

jobs:
  build:
    name: Build (transformers)
    if: >-
      github.event_name == 'push' ||
      inputs.step == 'build' ||
      inputs.step == 'publish'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: build/Docker/transformers/Dockerfile
          target: final
          push: false
          cache-from: type=gha
          cache-to: type=gha,mode=max
          secrets: |
            hf_token=${{ secrets.HF_TOKEN }}

  test:
    name: Test (transformers)
    needs: build
    if: >-
      !cancelled() &&
      needs.build.result != 'failure' &&
      ( github.event_name == 'push' ||
        inputs.step == 'test' ||
        inputs.step == 'publish' )
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v4
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: build/Docker/transformers/Dockerfile
          target: test
          load: true
          tags: gca-llm-transformers-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
          secrets: |
            hf_token=${{ secrets.HF_TOKEN }}
      - run: docker run --rm gca-llm-transformers-test:latest

  publish:
    name: Publish (transformers)
    needs: [build, test]
    if: >-
      github.event_name == 'push' ||
      inputs.step == 'publish'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v5
      - uses: docker/setup-buildx-action@v4
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v7
        with:
          context: .
          file: build/Docker/transformers/Dockerfile
          target: final
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/gca-llm-transformers:${{ github.ref_type == 'tag' && github.ref_name || 'latest' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          secrets: |
            hf_token=${{ secrets.HF_TOKEN }}
