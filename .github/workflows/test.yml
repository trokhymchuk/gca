name: Unit Tests

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v7
      - run: uv run --no-dev --group dev-no-llm pytest -m "not integration"
