name: Docker Static — Test

on:
  workflow_dispatch:
  workflow_run:
    workflows: ["Docker Static — Build"]
    types: [completed]

jobs:
  test:
    name: Test (static)
    if: github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/build-push-action@v6
        with:
          context: .
          file: build/Docker/static/Dockerfile
          target: test
          load: true
          tags: gca-static-test:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      - run: docker run --rm gca-static-test:latest
